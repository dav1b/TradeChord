name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
      - run: pip install -r requirements-dev.txt
      - run: python3 carousel/generate.py carousel/briefs/example-brief.yaml --validate-only
      - run: python3 -m unittest discover -s tests -v
      - run: python3 tests/render_smoke.py
